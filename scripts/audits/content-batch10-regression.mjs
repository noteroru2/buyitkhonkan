import fs from 'node:fs';import path from 'node:path';
import {ARCHITECTURE_PAGES} from '../../src/data/architecture/index.js';import {GUIDE_ARCHITECTURE} from '../../src/data/architecture/guides.js';import {HUB_ARCHITECTURE} from '../../src/data/architecture/hubs.js';import {HIGH_INTENT_GUIDE_CONTENT} from '../../src/data/highIntentGuideContent.js';
const root=process.cwd();const slugs=['วิธีเช็กรุ่นโน้ตบุ๊กก่อนขาย','วิธีดูสเปกโน้ตบุ๊กก่อนประเมิน','วิธีเช็กสุขภาพแบตโน้ตบุ๊ก','โน้ตบุ๊กไม่มีกล่องราคาตกไหม','วิธีเตรียม-macbook-ก่อนขาย','วิธีดู-cycle-count-macbook','macbook-ไม่มีที่ชาร์จราคาตกไหม','วิธีปลด-find-my-mac-ก่อนขาย','วิธีเช็ก-iphone-ก่อนขาย','วิธีดู-battery-health-iphone','iphone-จอเปลี่ยนราคาตกไหม','วิธีเตรียม-ipad-ก่อนขาย','ipad-cellular-vs-wifi-ราคามือสอง','วิธีเช็กสเปกคอมก่อนขาย','psu-มีผลต่อราคาคอมมือสองไหม','วิธีเช็กการ์ดจอก่อนขาย','การ์ดจอไม่มีประกันราคาตกไหม','วิธีเช็กกล้องก่อนขาย','วิธีเตรียม-ps5-ก่อนขาย','dead-pixel-มีผลต่อราคาจอไหม'];const set=new Set(slugs);const pages=GUIDE_ARCHITECTURE.filter(p=>set.has(p.slug));const missing=slugs.filter(s=>!HIGH_INTENT_GUIDE_CONTENT[s]);const notIndex=pages.filter(p=>p.lifecycle!=='INDEX').map(p=>p.slug);const priceHub=HUB_ARCHITECTURE.find(p=>p.id==='hub-price');const report={verdict:pages.length===20&&missing.length===0&&notIndex.length===0&&priceHub?.lifecycle==='INDEX'?'PASS':'FAIL',generatedAt:new Date().toISOString(),batch:'CONTENT_BATCH_10_REGRESSION_HISTORICAL',findings:{batch10Pages:pages.length,missingContent:missing.length,notIndex:notIndex.length,priceHubIndex:priceHub?.lifecycle==='INDEX',currentArchitectureIndex:ARCHITECTURE_PAGES.filter(p=>p.lifecycle==='INDEX').length,currentGuideIndex:GUIDE_ARCHITECTURE.filter(p=>p.lifecycle==='INDEX').length},missing,notIndex};const out=path.join(root,'docs/content-batch10');fs.mkdirSync(out,{recursive:true});fs.writeFileSync(path.join(out,'content-batch10-regression.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));if(report.verdict!=='PASS')process.exitCode=1;
